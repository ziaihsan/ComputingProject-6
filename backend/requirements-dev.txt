# Testing dependencies (SEPARATE from production requirements.txt)
# Install with: pip install -r requirements-dev.txt

# Testing framework
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=4.1.0
pytest-mock>=3.12.0

# HTTP testing for FastAPI
httpx>=0.27.0

# Mocking HTTP requests (for Binance API mocking)
respx>=0.21.0

# Optional: Code quality
ruff>=0.1.0
